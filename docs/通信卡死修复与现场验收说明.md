# 通信卡死修复与现场验收说明

版本：1.1.0

日期：2026-08-06

## 1. 本次解决的问题

现场现象是 PLC/HMI 实际温度已经升高到约 50°C，但上位机长期停留在旧的 20–30°C；只有完全退出程序再打开才恢复。

旧实现可能出现这样的锁死链：同步 PLC 读取永久不返回 → 占住共享 I/O 锁和温度采集标志 → 关闭连接也等待同一条路径 → 自动重连无法建立真正独立的新会话。UI 继续保留最后一个数值，看起来像温度“卡住”。

v1.1.0 的恢复链为：

`单条指令硬超时 → 原子废弃旧连接代次 → 强制关闭旧 Socket → 新建独立 transport/锁 → 立即启动本代温度采样 → 新样本直接推送 UI`

即使旧的第三方同步调用暂时还未返回，它也没有资格接触新连接或把旧温度写回界面。

## 2. 现场可见行为

- 正常：卡片显示最新温度和绿色“在线”。
- 超过正常刷新宽限：保留最后有效值，但温度带 `⚠`，状态变为黄色“数据滞后”。
- 通信判定失败：立即显示离线/重连中；只有用户曾主动连接过的设备才自动重连。
- 新连接尚未取得第一条本代温度：旧值不得变回绿色实时值。
- 目标温度、三相电压或扩展寄存器读取失败：实际温度仍继续刷新，不再因此反复断开主连接。
- 实际温度寄存器为空、错误或非有限值：不发布假 0°C；0°C 和负温仍被视为合法值。

## 3. 软件验证证据

- Debug 编译：0 错误。
- Release 编译：0 错误。
- 自动化测试：15/15 通过。
- `git diff --check`：通过。
- 四个外部只读审查：Kimi、GLM、Grok、AGY 最终均未发现与“温度卡旧值且必须重启”有关的 P0/P1，并建议发布。

测试覆盖的是软件状态机与故障注入。它不能替代真实 FX3U-ENET-ADP、无线网桥和现场寄存器的验收。

## 4. 新 EXE 与旧目录

新程序发布到独立目录：

`publish/MitsubishiMonitor-1.1.0-communication-recovery-20260806/MitsubishiMonitor.Demo.exe`

不要继续从 `publish123` 或旧桌面快捷方式启动，否则运行的仍可能是旧版本。旧目录不会被自动覆盖或删除，便于回退。发布包不包含开发机的 `config.json`；首次运行可自动生成默认配置，现场已有配置应先备份并按原部署策略放到新 EXE 同目录。

## 5. 首次现场验收（只读）

首次验证只观察通信与显示，不执行 PLC 写入、输出点动作或工艺控制。

1. 关闭所有旧版本进程，确认任务管理器中没有残留 `MitsubishiMonitor.Demo.exe`。
2. 从上面的 v1.1.0 独立目录启动，确认文件版本为 `1.1.0.0`。
3. 核对 4 台 PLC 的 IP、5000 端口、温度寄存器类型（Word/DINT）和除数与现场 HMI 一致。
4. 连续对照上位机与 PLC/HMI 温度至少 30 分钟，记录时间、两边温度和偏差。
5. 在安全条件下只做网络故障模拟，例如短时断开无线网桥链路；不得操作 PLC 输出。观察卡片是否先变黄/离线，再自动重连并恢复为新温度。
6. 恢复后确认显示的是当前真实温度，而不是故障前旧值；确认温度日志继续产生且时间连续合理。
7. 四台设备同时在线运行至少一个完整工艺周期，关注是否存在周期性掉线、重连风暴或明显 UI 卡顿。

## 6. 若现场仍出现旧值

请保留故障时间点前后日志，并记录：设备编号、PLC/HMI 实际显示、上位机显示、最后更新时间、卡片状态、网络是否可 Ping、是否自动重连成功。重点搜索诊断记录中的：

- `MitsubishiPlcService:IoTimeout`
- `MitsubishiPlcService:IoFailure`
- `DeviceManagerService:TemperatureStale`
- `自动重连`

如果日志显示通信持续成功且每次都读到 20–30°C，而 PLC/HMI 为 50°C，应优先核对两端读取的寄存器地址、Word/DINT 类型、缩放除数以及 PLC 程序是否更新同一个寄存器；这属于点表/现场数据源问题，不是 WPF 刷新卡死。

## 7. 已知非阻断项

- LiveCharts 0.9.7 在 .NET 8 下仍有 `NU1701` 兼容性警告；本次未升级图表库，以免扩大通信修复范围。
- `app.manifest` 仍有 `WFAC010` 高 DPI 指导警告，不影响本次通信恢复逻辑。
- HslCommunication 的真实 Socket 强制关闭行为仍需在 FX3U 与无线网桥上验证；这是最终现场验收边界。

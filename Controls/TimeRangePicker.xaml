<UserControl x:Class="MitsubishiMonitor.Demo.Controls.TimeRangePicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:MitsubishiMonitor.Demo.Converters"
             mc:Ignorable="d"
             d:DesignHeight="50" d:DesignWidth="700">
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <Border Background="#0D1520" BorderBrush="#1E2D3D" BorderThickness="1" CornerRadius="6" Padding="10,6">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

            <!-- 预设按钮组 -->
            <TextBlock Text="时间：" FontSize="12" Foreground="#7D8590" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ItemsControl ItemsSource="{Binding Presets, RelativeSource={RelativeSource AncestorType=UserControl}}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding}" Margin="0,0,4,0" Padding="10,4" FontSize="12" Cursor="Hand"
                                Command="{Binding DataContext.SelectPresetCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="#161B22"/>
                                    <Setter Property="Foreground" Value="#7D8590"/>
                                    <Setter Property="BorderBrush" Value="#30363D"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Bd" Property="Background" Value="#1A2D3D"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding DataContext.SelectedPreset, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="{Binding}">
                                            <Setter Property="Background" Value="#238636"/>
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="BorderBrush" Value="#2EA043"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- 自定义时间（仅自定义模式显示） -->
            <StackPanel Orientation="Horizontal" Margin="8,0,0,0"
                        Visibility="{Binding IsCustomMode, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVisibilityConverter}}">
                <DatePicker SelectedDate="{Binding StartDate, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            Width="120" Margin="0,0,6,0" Background="#161B22" Foreground="#C9D1D9"/>
                <TextBlock Text="至" VerticalAlignment="Center" Foreground="#7D8590" Margin="0,0,6,0" FontSize="12"/>
                <DatePicker SelectedDate="{Binding EndDate, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            Width="120" Background="#161B22" Foreground="#C9D1D9"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>

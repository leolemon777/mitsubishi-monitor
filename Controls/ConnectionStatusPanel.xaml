<UserControl x:Class="MitsubishiMonitor.Demo.Controls.ConnectionStatusPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="160" d:DesignWidth="320">

    <Border Background="#0D1520" BorderBrush="#1E2D3D" BorderThickness="1" CornerRadius="8" Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 标题行 -->
            <Grid Grid.Row="0" Margin="0,0,0,12">
                <StackPanel Orientation="Horizontal">
                    <Ellipse Width="10" Height="10" VerticalAlignment="Center" Margin="0,0,8,0">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="#3FB950"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsConnected, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="False">
                                        <Setter Property="Fill" Value="#F85149"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Text" Value="已连接"/>
                                <Setter Property="Foreground" Value="#3FB950"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsConnected, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="False">
                                        <Setter Property="Text" Value="未连接"/>
                                        <Setter Property="Foreground" Value="#F85149"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
                <TextBlock Text="连接状态" HorizontalAlignment="Right" FontSize="12" Foreground="#484F58" VerticalAlignment="Center"/>
            </Grid>

            <!-- 统计信息 -->
            <Grid Grid.Row="1" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="连接时长" FontSize="11" Foreground="#7D8590" Margin="0,0,0,4"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding ConnectionDurationDisplay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           FontSize="11" Foreground="#C9D1D9" HorizontalAlignment="Right" Margin="0,0,0,4" FontFamily="Consolas"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="发送数据包" FontSize="11" Foreground="#7D8590" Margin="0,0,0,4"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding PacketsSent, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           FontSize="11" Foreground="#C9D1D9" HorizontalAlignment="Right" Margin="0,0,0,4" FontFamily="Consolas"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="最后通信" FontSize="11" Foreground="#7D8590"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding LastCommunicationDisplay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           FontSize="11" Foreground="#C9D1D9" HorizontalAlignment="Right" FontFamily="Consolas"/>
            </Grid>

            <!-- 重连按钮 -->
            <Button Grid.Row="2" Content="重新连接"
                    Command="{Binding ReconnectCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    HorizontalAlignment="Right" Padding="12,5" FontSize="12"
                    Background="#161B22" Foreground="#58A6FF" BorderBrush="#30363D" BorderThickness="1"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="4" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#1A2D3D"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</UserControl>

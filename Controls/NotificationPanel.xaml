<UserControl x:Class="MitsubishiMonitor.Demo.Controls.NotificationPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="320">

    <Border Background="#0D1520" BorderBrush="#1E2D3D" BorderThickness="1" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Border Grid.Row="0" Background="#161B22" BorderBrush="#21262D" BorderThickness="0,0,0,1"
                    CornerRadius="8,8,0,0" Padding="12,8">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="通知" FontSize="13" FontWeight="SemiBold" Foreground="#E0E0E0" VerticalAlignment="Center"/>
                        <Border Background="#58A6FF" CornerRadius="8" Padding="5,1" Margin="8,0,0,0" VerticalAlignment="Center"
                                Visibility="{Binding UnreadCount, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource CountToVisibilityConverter}}">
                            <TextBlock Text="{Binding UnreadCount, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                       FontSize="10" Foreground="White" FontWeight="Bold"/>
                        </Border>
                    </StackPanel>
                    <Button Content="清除全部" HorizontalAlignment="Right" FontSize="11" Padding="8,3"
                            Background="Transparent" Foreground="#7D8590" BorderThickness="0" Cursor="Hand"
                            Command="{Binding ClearAllCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                </Grid>
            </Border>

            <!-- 通知列表 -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" MaxHeight="350">
                <ItemsControl ItemsSource="{Binding Notifications, RelativeSource={RelativeSource AncestorType=UserControl}}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#21262D" BorderThickness="0,0,0,1" Padding="12,10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding TypeIcon}" FontSize="16"
                                               VerticalAlignment="Top" Margin="0,0,10,0" Foreground="#58A6FF"/>
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding Title}" FontSize="12" FontWeight="SemiBold"
                                                   Foreground="#E0E0E0" Margin="0,0,0,3"/>
                                        <TextBlock Text="{Binding Message}" FontSize="11" Foreground="#7D8590"
                                                   TextWrapping="Wrap" Margin="0,0,0,3"/>
                                        <TextBlock Text="{Binding TimeDisplay}" FontSize="10" Foreground="#484F58"/>
                                    </StackPanel>
                                    <Button Grid.Column="2" Content="✕" FontSize="11" Padding="6,3"
                                            Background="Transparent" Foreground="#484F58" BorderThickness="0"
                                            Cursor="Hand" VerticalAlignment="Top"
                                            Command="{Binding DataContext.DismissCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- 空状态 -->
            <TextBlock Grid.Row="1" Text="暂无通知" FontSize="12" Foreground="#484F58"
                       HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,20">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Notifications.Count, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="0">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <!-- 底部 -->
            <Border Grid.Row="2" Background="#161B22" BorderBrush="#21262D" BorderThickness="0,1,0,0"
                    CornerRadius="0,0,8,8" Padding="12,6">
                <TextBlock FontSize="11" Foreground="#484F58">
                    <Run Text="共 "/>
                    <Run Text="{Binding Notifications.Count, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                    <Run Text=" 条通知"/>
                </TextBlock>
            </Border>
        </Grid>
    </Border>
</UserControl>
